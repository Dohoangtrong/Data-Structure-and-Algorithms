#include <bits/stdc++.h>
using namespace std;

int main(){
	int n;
	cin >> n;
	while(n--){
		int m;
		cin >> m;
		vector<int> v;
		int a;
		for ( int i = 0; i < m; i++){
			cin >> a;
			v.push_back(a);
		}
		sort(v.begin(),v.end());
		for ( int i = 0; i < m; i++){
			cout << v[i] << " ";
		}
		cout << endl;
	}
}
