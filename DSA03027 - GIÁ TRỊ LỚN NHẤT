#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int n;
	cin >> n;
	int ar[n];
	long long s = 0;
	for (int i = 0; i < n; i++){
		cin >> ar[i];
		if ( ar[i] > 0 ){
			s += ar[i];
		}
	}
	cout << s*2 << endl;
}
